{{ form_start(form) }}
{{ form_row(form.typeMouvement) }}
{{ form_row(form.service) }}
{{ form_row(form.fonction) }}
{{ form_row(form.grade) }}
{{ form_row(form.dateEffet) }}
{{ form_row(form.numeroDecision) }}
{{ form_row(form.dateDecision) }}
{{ form_row(form.observations) }}
<div class="mt-3 d-flex gap-2">
    <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg"></i> Enregistrer</button>
    <a href="{{ path('admin_personnel_show', {id: personnel.id}) }}" class="btn btn-outline-secondary">Annuler</a>
</div>
{{ form_end(form) }}
