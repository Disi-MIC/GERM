{{ form_start(form) }}
{{ form_row(form.code) }}
{{ form_row(form.libelle) }}
{{ form_row(form.actif) }}
<div class="mt-3 d-flex gap-2">
    <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg"></i> Enregistrer</button>
    <a href="{{ path('admin_liste_valeur_index', {categorie: categorie.value}) }}" class="btn btn-outline-secondary">Annuler</a>
</div>
{{ form_end(form) }}
